# Pinned to lines that ship prebuilt wheels for the base image's Python 3.13
# (avoids source builds — psycopg2 2.9.9 had no 3.13 wheel and needed pg_config).
kafka-python>=2.2.0,<3
prometheus-client>=0.20.0,<1
psycopg2-binary>=2.9.10,<3
